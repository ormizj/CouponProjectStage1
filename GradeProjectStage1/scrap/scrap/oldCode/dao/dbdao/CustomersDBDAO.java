package scrap.oldCode.dao.dbdao;

public class CustomersDBDAO {

//	/**
//	 * Helper method, to get the first name from a <code>String</code> or an empty
//	 * <code>String</code> if there is none.
//	 * 
//	 * @param name
//	 * @return The <code>String</code> segment before the occurrence of the first
//	 *         space.
//	 */
//	private static String getFirstName(String name) {
//		String firstName = "";
//		for (int i = 0; i < name.length(); i++) {
//			if (name.charAt(i) == ' ') {
//				break;
//			}
//			firstName += name.charAt(i);
//		}
//		return firstName;
//	}
//
//	/**
//	 * Helper method, to get the last name from a <code>String</code> or an empty
//	 * <code>String</code> if there is none.
//	 * 
//	 * @param name
//	 * @return The <code>String</code> segment after the occurrence of the first
//	 *         space.
//	 */
//	private static String getLastName(String name) {
//		String lastName = "";
//		boolean space = false;
//		for (int i = 0; i < name.length(); i++) {
//			if (name.charAt(i) == ' ') {
//				if (!space) {
//					space = true;
//					continue;
//				}
//			}
//			if (space)
//				lastName += name.charAt(i);
//		}
//		return lastName;
//	}

}
